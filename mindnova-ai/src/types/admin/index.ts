// ─── Admin Types — src/types/admin/index.ts ──────────────────────────────────
// Re-exports shared metric/chart types from src/types for the ads-hourly feature.

export type {
  MetricKey,
  MetricType,
  MetricConfig,
  HourlyDataPoint,
  HourStat,
  CardStats,
  CardState,
  DragRange,
} from "../index";
